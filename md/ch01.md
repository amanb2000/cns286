# Chapter 1: Mathematics of Prediction

Home: [Course Index](index.html)

_[Aman Bhargava](aman-bhargava.com), Fall 2024_

Probability distributions quantify the likelihood of events. 
Modern language models are chiefly concerned with approximating the probability distribution of sequences of well-formed text. 
Once a high-quality approximation of the probability distribution of text sequences is learned, text can be generated by sampling the distribution. 
A basic understanding of probability distributions ([Section 1.1](#probability-distributions)) is key to working with large language models. 
We will then provide an overview of basic methods (ML, MAP) for estimating probability distributions in [Section 1.2](#maximum-likelihood-estimation).





## 1.1: Probability Spaces {#probability-spaces}

**Definition**: Probability space -- a probability space is a mathematical framework to model random experiments that consists of three elements: 

 1. **Sample space $\Omega$**: The set of all possible outcomes of a random experiment. 
 2. **Sigma-algebra ($\mathcal F$)**: A collection of subsets of $\Omega$ including $\Omega$ itself and the empty set $\varnothing$ that satisfies the axioms of $\sigma$-algebras (e.g., closure under complement and countable unions), representing the events or "outcomes" we can assign probabilites to. 
 3. **Probability measure ($P$)**: A function $P: \mathcal F \to [0, 1]$ that assigns probabilities to events in $\mathcal F$. 

The probability measure $P$ must satisfy the **Kolmogorov axioms**: 

 1. **Non-negativity**: For any event $A \in \mathcal F$, $$P(A) \geq 0$$
 2. **Normalization**: $$P(\Omega) = 1$$. 
 3. **Countable Additivity**: For any countable sequence of pairwise disjoint events $\{A_i\}_{i=1}^\infty \subset \mathcal F$ (i.e. $A_i \cap A_j = \varnothing$ for $i\neq j$).

These axioms set the stage for deriving important properties of probability distributions. E.g., for any two events $A, B \in \mathcal F$, $$P(A \cup B) = P(A) + P(B) - P(A \cap B)$$


### Random Variables

**Definition**: Random variable -- a random variable is a measurable function that maps outcomes from a random experiment to real numbers: $$X: \Omega \to \mathbb R$$ 
Random variables quantify random phenomena and are central to probability and statistics. 

 - **Discrete random variables**: Take on countable values (e.g., integers). 
 - **Continuous random variables**: Take on values from an interval or collection of intervals in $\mathbb R$. 

**Notation**: Statistics and probability theory might has irritating notation, but we are stuck with it. 

 - **Uppercase letters** (e.g., $X$) denote random variables. 
 - **Lowercase letters** (e.g., $x$) denote specific values or "realizations" of the random variable $X$. We might say $x$ is sampled from $X$ or a function of $X$ as $x\sim X$ or $x \sim g(X)$. You may also see $x\sim f_X(x)$ or $x\sim p_X(x)$ when attention is being drawn to the probability distribution of random variable $X$. 

**Definition**: Support of random variable $S_X$ -- the support or range of a random variable $X$ is 
$$S_X = \{x \in \mathbb R \mid \exists \omega \in \Omega \text{ such that } X(\omega) = x\}$$


## 1.2 Probability Distributions

A probability distribution describes how probabilities are assigned over the potential values of a random variable. 

### Discrete Random Variables and Probability Mass Functions (PMFs)

For a discrete random variable $X$ with support $S_X$, the **probability mass function** (PMF) $p_X(x)$ gives the probability $\Pr\{X = x\}$. PMFs obey the following properties: 

 1. **Non-negativity**: $p_X(x) \geq 0$  $\forall$ $x \in S_X$. 
 2. **Normalization**: $$\sum_{x\in S_X} p_X(x) = 1$$
 3. **Additivity**: For any countable collection of disjoint values $\{x_i\}$, $$\Pr \big( X \in \{x_i\} \big) = \sum_{x_i} p_X(x_i)$$


### Continuous Random Variables and Probability Density Functions

For a continuous random variable $X$, the **probability density function** (PDF) $f_X(x)$ is a function such that the probability such that $X$ falls within interval $[a, b]$ is given by 
$$\Pr\big(a \leq X \leq b\big) = \int_a^b f_X(x) dx$$

**Properties of PDFs**: 

 1. **Non-negativity**: $f_X(x) \geq 0$ $\forall x \in \mathbb R$
 2. **Normalization**: $$\int_{-\infty}^{\infty} f_X(x) dx = 1$$
 3. **Probability over intervals**: Probabilities are calculated over intervals, not at individual points as applying $\Pr\big(a \leq X \leq b\big) = \int_a^b f_X(x) dx$ to an interval of zero length gives zero probability. 

The name for the value of the PDF $f_X(x)$ at a particular value of $x$ is called **"likelihood"** rather than **"probability"**. 
If you confuse these, it will anger the statisticians. 


### Laws of Large Numbers


Recall the definitions of expected value for continuous random variable $X$ is $\mu = \mathbb E[X] = \int_{-\infty}^{\infty} x \cdot f_X(x)$. For $X$, we have $\mu = \mathbb E[X] = \sum_{x\in S_X} x\cdot p_X(x)$. 


**Weak law of large numbers** (Khinchin's law): Let $\{X_i\}_{i=1}^n$ be a sequence of _independent and identically distributed_ (i.i.d.) random variables with finite expected value $\mu = \mathbb E[X_i]$. Then, for any $\epsilon > 0$, 
$$
\lim_{n\to \infty} \Pr\begin{pmatrix}
\big|
\frac 1 n \sum_{i=1}^n X_i - \mu
\big|
\geq \epsilon
\end{pmatrix} = 0
$$

**Strong law of large numbers**: If $\{x_i\}_{i=1}^n$ are sampled from i.i.d. random variables with mean $\mu = \mathbb E[X_i]$, then the probability of the sample mean = $\mu$ is $1$ as $n \to \infty$. 
$$
\Pr\begin{pmatrix}
\lim_{n\to \infty} \frac 1 n \sum_{i=1}^n X_i = \mu
\end{pmatrix} = 1
$$


### Conditional Distributions

Joint distributions for two random variables are denoted as $f_{XY}(x, y)$ for continuous random variables and $p_{XY}(x, y)$. 
For brevity, we will refer to each without the subscripts as $f(x, y)$ and $p(x, y)$. 
Conditional distributions are denoted $f(y | x)$ or $p(y | x)$ and are interpreted as the probability distribution over $y$ given a known value of $x$. 

$$
f(y | x) = f(x, y) / f(x)
$$

$$
p(y | x) = p(x, y) / p(x)
$$

**Independence**: Random variables $X, Y$ are independent iff $p(x, y) = p(x) p(y)$. This also implies that $p(y | x) = p(y)$. 

**Conditional Independence**: 



### Chain Rule of Probability

Let us break free from the assumption of independence in a sequence of random variables $\{X_i\}_{i=1}^n$. 
If we know that the process underlying $\{X_i\}$ is **causal** -- i.e., $X_i$ is conditional only on $X_1, \dots, X_{i-1}$, then we can use this handy trick to factor the joint probability of the sequence $p(x_1, \dots, x_n)$. 
as 

$$
p(x_1, \dots, x_n) = \prod_{i=1}^n p(x_i | x_{< i})
$$

where $x_{< i}$ denotes $x_1, \dots, x_{i-1}$. The chain rule of probability is especially handy for modelling language as a random process. 


**Derivation**: By repeatedly applying the chain rule of probability and invoking our causal assumption ($X_i$ is only conditional on $X_{< i}$), we get 

$$
p(x_1) = p(x_1)
$$

$$
p(x_1, x_2) = p(x_2 | x_1) p(x_1)
$$

$$
p(x_1, x_2, x_3) = p(x_3 | x_1, x_2) p(x_1, x_2)
$$

$$
 = p(x_3 | x_1, x_2) \underbrace{p(x_2 | x_1) p(x_1)}_{\equiv p(x_1, x_2)}
$$

And so on, until we derive the general form. 

**Note**: While I have used discrete/PMF notation $p(x_1, \dots, x_n)$, the chain rule also applies to causal random processes with continuous variables $f(x_1, \dots, x_n)$. 




## 1.2: Estimating Probability Distributions

Abstract probability distributions like $p(x)$, $p(y | x)$, and $p(x_i | x_1,\dots, x_{i-1})$ are great to think about, but we often are not given the exact form of $p(x)$ initially. 
More commonly, we have a bunch of samples from the distribution in question, and we need to estimate $p(x)$ from samples $x_i \sim p(x)$ or $x_i \sim f(x)$. 

**Parameterized probability distributions**: The default variable to represent the parameters of a probability distribution is $\theta$, and the parameterized distribution as $p(x; \theta)$ or $f(x; \theta)$. You may also see the equivalent notation $p_{\theta}(x)$ or $f_{\theta}(x)$ commonly used. 


**Goal of parameter estimation**: Once we have figured out which family of distributions we are interested in, we want to estimate the best parameters $\hat \theta$ that explain our measurements, where $\hat \theta$ is an **estimator** for the true parameter $\theta$. 

**Types of estimators**: Once you derive an estimator, these concepts are useful for understanding how good you can expect your estimator to be. Let $\hat \theta_n$ represent the estimator of $\theta$ given $n$ samples from the "ground truth" distribution (e.g., experimental measurements). 

 1. **Estimation error**: $\hat \theta_n - \theta$
 2. **Bias of estimator**: $\mathbb E[\hat \theta_n] - \theta$
     - Unbiased estimator: $\mathbb E[\hat \theta_n] = \theta$ for all $\theta$.
     - Asymptotically unbiased estimator: $\lim_{n\to \infty} \mathbb E[\hat \theta_n] = \theta$ for all $\theta$.
 3. **Consistency**: An estimator is consistent if $\lim_{n\to \infty} \hat \theta_n = \theta$.

**Simplest parameter estimation example**: Let's say we have a bunch of i.i.d. samples $x_1, \dots, x_n$ sampled from some unknown Bernoulli random variable $X$. 
Let $r$ denote the "ground truth" probability of succcess for a given Bernoulli
trial, so $P(X = 1) = r$ and $P(X = 0) = 1-r$. 
The variable $r$ **parameterizes** our distribution, and our goal is to estimate
$r$ from the samples. 
Intuitively, you might see that the sample mean $\hat \theta_n = \frac 1 n \sum_{i=1}^n x_i$ is a good estimator for $r$.
We can make this more rigorous, and also expand our estimation techniques to include a **prior distribution** over the parameters of our model, using maximum likelihood (ML) and maximum a posteriori (MAP) estimation. 
Machine learning tends to use a lot of maximum likelihood estimation -- if you've ever used cross-entropy loss, you've done a form of ML estimation. 
MAP estimation is still good to know about and comes up a more frequently in statistical learning. 

### Maximum Likelihood Estimation

**Setup**: Let random variable $\mathbf X = [X_1, X_2, \dots, X_n]$. Then for discrete $X_i$ we would have joint PMF $p_{\mathbf X} (\mathbf x; \theta) = p_{\mathbf X}(x_1, \dots, x_n; \theta)$ for discrete random variables or joint PDF $f_{\mathbf X}(\mathbf x; \theta)$. 

The vector $\mathbf x$ represents a series of measurements. Then the **maximum likelihood estimate** of $\theta$ is

$$
\hat \theta_n = \arg\max_{\theta} p_{\mathbf X}(\mathbf x; \theta)
$$

For i.i.d. $X_1, \dots, X_n$, we can apply the independence property to get 

$$
\hat \theta_n = \arg\max_{\theta} \prod_{i=1}^n p_X(x_i; \theta)
$$


**Log transformation trick**: The computationalists among you might be getting nervous because the product of many probabilities $p_X(x_i; \theta) \in [0, 1]$ will become very small -- too small to represent properly with floats! 
Fortunately you can just take the $\log$ of the product we want to maximize. 
Since $\log(z)$ is a monotonic function of $z$, maximizing $\log(z)$ is equivalent to maximizing $z$ itself. 
Applying the logarithm, 

$$
\hat \theta_n = \arg\max_{\theta} \sum_{i=1}^n \log p_X(x_i; \theta)
$$

Instead of dealing with products over near-zero numbers, we now deal with the sum over (potentially very large) negative numbers arising from $\log p_X(\cdot)$. 
The high-anxiety computationalists among you might *still* be worried, since we still face numerical instability when $p_X(\cdot) = 0$ leading to $\log 0 = -\infty$. 
The standard fix for that is to add a small number $\epsilon$ (often $\epsilon = 10^-9$) to $p_X(\cdot)$ before the logarithm. 
That way, as long as $p_X(\cdot) \in [0, 1]$ and $\log p_X(\cdot) \in [-\infty, 0]$, we will have $\log(p_X(\cdot)+\epsilon) \in [\log(\epsilon), \log(1+\epsilon)] \approx [-6, 0]$.


**Deriving ML Estimate for Bernoulli Random Variable Parameter**: Recall our setup with a bunch of i.i.d. samples $\mathbf x = [x_1, \dots, x_n]$ from a Bernoulli random variable $X$ with unknown rate of success $r$. 
Then we can apply the i.i.d. case of ML estimation, replacing generic $\theta$ with $r$: 

$$
\hat r_n = \arg\max_{r} \prod_{i=1}^n p_X(x_i; r)
$$

$$
\hat r_n = \arg\max_{r} \prod_{i=1}^n \begin{cases}
    r & \text{ if } x_i = 1 \\
    1-r & \text{ if } x_i = 0
\end{cases}
$$

Let us denote the number of successful outcomes as $m$. Then we get 

$$
\hat r_n = \arg\max_r \underbrace{r^m (1-r)^{n-m}}_{g(r; n, m)}
$$

To find the extreme points of $g(r; n, m)$, let's differentiate w.r.t. $r$: 

$$
\frac{d}{dr} g(r; n, m) = m r^{m-1} (1-r)^{n-m} - r^m (n-m)(1-r)^{n-m-1}
$$

Solving for zero, assuming non-zero $r$: 

$$
m r^{m-1} (1-r)^{n-m} = r^m (n-m)(1-r)^{n-m-1}
$$


$$
m (1-r) = r (n-m)
$$

$$
\frac{1-r}{r} = \frac{n-m}{m}
$$

$$
\frac{1}{r} = \frac{n}{m}
$$

Therefore, $r = m/n$, just as suggested by our intuition. 
Unlike our intuition, this approach allows us to calculate maximum likelihood estimators as long as we have some samples from the true distribution and a sense of the family of PMFs or PDFs to which the true distribution belongs. 

_Bonus: [log-sum-exp](#<<>>) trick_

**Exercise 1.1**: Derive the maximum likelihood estimator for i.i.d. samples from the normal distribution with unknown mean $\mu$ and standard deviation $\sigma$, so $x_i \sim \mathcal N(\mu, \sigma)$ for $i=1, \dots, n$. Recall the form of the scalar Gaussian distribution: 

$$
f_X(x) = \frac{1}{2\pi \sigma^2} \exp\begin{pmatrix}
\frac{-(x-\mu)^2}{2\sigma^2}
\end{pmatrix}
$$
$$
\iff x \sim \mathcal N(\mu, \sigma)
$$

Provide the equations for maximum likelihood estimators $\hat \mu$, $\hat \sigma$ as a function of $\{x_i\}_{i=1}^n$. 


### Maximum a Posteriori Estimation

ML estimation is great in the limit of large $n$, but for few samples, the model might do some silly things. 
Maximum a posteriori estimation incorporates a **prior distribution** over parameter $\theta$ in the estimation process. 
Instead of maximizing the likelihood of the observed data given the parameter (as in ML estimation), maximum a posteriori (MAP) estimation maximizes the **probability of the parameters given the observed data**. 
To do this, we apply Bayes theorem and our prior over $\theta$ to flip from $P(x; \theta)$ to $P(\theta | x)$. 

**Bayes Theorem**: $$P(B | A) = P(A | B) \frac{P(B)}{P(A)}$$

**Maximum a Posteriori Estimation**: Let $\hat \theta_{MAP}$ represent the maximum a posteriori estimate of parameter $\theta$ given samples $\{x_i\}_{i=1}^N$. Let $p(\theta)$ represent our prior distribution over $\theta$, and similar for $p(x | \theta)$ and $p(x)$. Then 

$$
\hat \theta_{MAP} = \arg\max_{\theta} f(\theta | \{x\}_{i=1}^N)
$$

$$ 
\iff  \hat \theta_{MAP} = \arg\max_{\theta} f(\{x_i\}_{i=1}^N | \theta) \frac{f(\theta)}{f(\{x_i\}_{i=1}^N)}
$$

As before, the optimization objective works for PMFs $p(\cdot)$ as well as PDFs $f(\cdot)$. 


**Exercise 1.2**: Which do you think would be easier to implement computationally -- ML or MAP parameter estimation? Why? _Hint: think about how you would compute each term in the optimization for each given some computational model for $f(x; \theta)$_




## 1.3: Shannon N-Gram Models

Congratulations, you have acquired the basics of mathematical prediction! 
We have the tools to rigorously understand some of the earliest computational language models in history: Shannon's N-grams!
As citizens of the information age, we bask in the reflected glory of Shannon's "greatest masters thesis of all time" on information theory. 
As he was formalizing his theories of communication over noisy channels and the like, Shannon was actively experimenting with ways to model the probability distribution of language.

Language is tricky, since the number of possible $k$-long sequences of characters from a 26 character alphabet is, well, $26^k$ (a big number -- so big that some dramatic scientists call it an "explosion"!).
Since modern computers had yet to be developed, Shannon started by ignoring the fact that all previous characters $x_1, \dots, x_{k-1}$ in a sequence could concievably change the probability of the next character $x_k$. 
Instead, he restricted his models to N-grams -- what's the probability $p_{\theta}(x_N | x_1, \dots, x_{N-1})$? 


### 1-gram model

The 1-gram model is a great starting point because we are just interested in modelling the PMF $p_{\theta}(x)$. 
The simplest way to parameterize this is as a **categorical distribution** -- a generalization of the Bernoulli distributions we talked about earlier. 
$\theta$ just needs to have 26 values representing the probability of each character in English. 
Just like with the Bernoulli distribution, if we have no prior and want to do ML estimation, we can just take the frequency of each character in our corpus as the probability of that character. 
If you really want you can re-derive it, but it's basically the same as before. 

Now we have a very simple model of language, and it's good enough to do some interesting stuff with "typical sequences" and channel capacity. 
But if you sample sequences $x_i \sim p(x_i)$, you get gibberish because it cannot take **any** context into account: 

```
1-gram approximation of english (symbols independent but with frequencies of English text): 

    OCRO HLI RGWR NMIELWIS EU LL NBNESEBYA TH EEI ALHENHTTPA OOBTTVA NAH BRL
```


### 2-gram model

The 2-gram model tries to estimate $p_{\theta}(x_N | x_{N-1})$. 
You can think of $\theta$ as a big table or matrix. 

$$
\begin{bmatrix}
    p(a | a) & p(b | a) & \dots & p(z | a) \\
    p(a | b) & p(b | b) & \dots & p(z | b) \\
    \vdots & \vdots & \ddots & \vdots \\
    p(a | z) & p(b | z) & \dots & p(z | z) 
\end{bmatrix}
$$

Now we need to estimate $26^2 = 676$ frequencies from our dataset. 
We can just calculate the frequency of each of the 676 bigrams in English, which would give us the following matrix: 

$$
\begin{bmatrix}
    p(a, a) & p(b, a) & \dots & p(z, a) \\
    p(a, b) & p(b, b) & \dots & p(z, b) \\
    \vdots & \vdots & \ddots & \vdots \\
    p(a, z) & p(b, z) & \dots & p(z, z) 
\end{bmatrix}
$$

Then normalize each row to get back to the desired original conditional distribution matrix. 

```
2-gram model of English: 

    ON IE ANTSOUTINYS ARE T INCTORE ST BE S DEAMY ACHIN D ILONASIVE TUCOOWE AT TEASONARE FUSO TIZIN ANDY TOBE SEACE CTISBE
```

Still not quite GPT-4, but it looks a bit more like normal English. 


### 3-gram model

Hopefully you're starting to see the pattern -- for the 3-gram model, we need to estimate the probabilities of $26^3 = 17576$ 3-grams. 
Despite his lack of modern computing technology, Claude managed it: 

```
3-gram model of English: 

    IN NO IST LAT WHEY CRATICT FROURE BIRS GROCID PANDENOME OF DEMONSTURES OF THE REPTAGIN IS REGOACTIONA OF CRE
```

Not a bad language model for pre-WWII!



### Beyond N-gram models

The **combinatorial explosion** of the number of parameters one must estimate for $N$-gram models makes them prohibitively expensive to scale. 
Fortunately, there's an entire discipline called "artificial intelligence" -- especially the subfield of machine learning -- that has made some progress on rendering problems with combinatorial explosions tractable!

Before we get onto that, why don't you go ahead and implement an N-gram model yourself. 

**Exercise 1.3**: Download a book you like from [Project Gutenberg](#<<>>) as a text file. Convert to lowercase, then implement a Python program to train $N$-gram models. I'd recommend using a dictionary data structure, and also implement [Laplace smoothing](#<<>>) (basically a uniform prior) for previously unseen $N$-grams. Make sure you make $N$ adjustable, and make it easy to save your trained model, load it, and sample from it. 

_Bonus_: Make a chat interface for talking with your N-gram models. 







